mscdocument automade;
language ASN.1;
data dataview-uniq.asn;
inst ground;
msg Takeoff : (MyReal);
msg dHeight : (MyReal);
msg Height : (MyReal);
msc recorded;
    gate in Takeoff,1(2.0) from ground;
    gate in Takeoff,2(2.0) from ground;
    gate in Takeoff,3(2.0) from ground;
    ground: instance;
        out Takeoff,1(2.0) to env;
        out Takeoff,2(2.0) to env;
        out Takeoff,3(2.0) to env;
    endinstance;
endmsc;
endmscdocument;
